@0xb4b0989588cc41a2;

using HandlerName = Text;

struct StructHandlerInfo {
    # pH datapoint with an associated timestamp in milliseconds
    structName @0 :Text;
    handlers @1 :List(HandlerName);
}
